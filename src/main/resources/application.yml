spring:
  profiles:
    default: local

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true


server:
  servlet:
    session:
      cookie:
        name: JSESSIONID
        http-only: true
        secure: true
        same-site: none
        max-age: ${A}
        domain: ${B}


