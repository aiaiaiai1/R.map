spring:
  profiles:
    default: local

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true


server:
  servlet:
    session:
      cookie:
        name: JSESSIONID
        domain: ${A}
        http-only: true
        secure: true
        same-site: ${B}
        max-age: ${C}

